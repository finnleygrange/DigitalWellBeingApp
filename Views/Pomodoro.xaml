<UserControl x:Class="DigitalWellBeingApp.Views.Pomodoro"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DigitalWellBeingApp.Views"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
            <RadioButton Content="Pomodoro" Width="150" HorizontalContentAlignment="Center" Style="{StaticResource NavButton}" IsChecked="True"/>
            <RadioButton Content="Short Break" Width="150" HorizontalContentAlignment="Center" Style="{StaticResource NavButton}" Margin="10,0"/>
            <RadioButton Content="Long Break" Width="150" HorizontalContentAlignment="Center" Style="{StaticResource NavButton}"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,20">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="25:00"
                   FontSize="96" FontWeight="Bold"
                   Foreground="#F8FAFC"
                   HorizontalAlignment="Center"/>
                <Button Content="START"
                Width="220" Height="60"
                FontSize="28" FontWeight="SemiBold"
                Foreground="White"
                Background="#4F46E5"
                BorderBrush="Transparent"
                HorizontalAlignment="Center"
                Cursor="Hand"/>
            </StackPanel>
        </Grid>


        <!--<Grid Grid.Row="3" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                <TextBlock Text="Pomodoro 1 of 4"
                           FontSize="20" FontWeight="Bold"
                           Foreground="#A5B4FC"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Focus time remaining before your next break."
                           FontSize="14" Foreground="#CBD5E1"/>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="Today's Tasks"
                           FontSize="20" FontWeight="Bold"
                           Foreground="#A5B4FC"
                           HorizontalAlignment="Center"/>
                <StackPanel>
                    <CheckBox Content="Finish UI for Pomodoro" Foreground="#F8FAFC" Margin="0,2"/>
                    <CheckBox Content="Write daily journal" Foreground="#F8FAFC" Margin="0,2"/>
                    <CheckBox Content="Take 5 min walk" Foreground="#F8FAFC" Margin="0,2"/>
                </StackPanel>
            </StackPanel>
        </Grid>-->

    </Grid>
</UserControl>
